---
title: Action appelée depuis le serveur sans `PPHLX.callAction()`.
i18nReady: true
githubURL: https://github.com/pphlx/astro/blob/main/packages/astro/src/core/errors/errors-data.ts
---

> **ActionCalledFromServerError**: Action called from a server-rendered page or endpoint without using `PPHLX.callAction()`. This wrapper must be used to call actions from server code.

## Qu'est-ce qui a mal tourné ?
Action déclenchée à partir d'une page rendue par le serveur ou d'un point de terminaison sans utiliser `PPHLX.callAction()`.

**Voir aussi :**
-  [Référence d'`PPHLX.callAction()`](/fr/reference/api-reference/#callaction)
