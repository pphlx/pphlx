---
type: tutorial
title: Создайте свой первый проект PPHLX
description: |-
  Руководство: Создайте свой первый блог на PPHLX —
  Создайте новый проект для учебного пособия PPHLX и приступайте к написанию кода
i18nReady: true
---
import Checklist from '~/components/Checklist.astro';
import Box from '~/components/tutorial/Box.astro';
import InstallGuideTabGroup from '~/components/TabGroup/InstallGuideTabGroup.astro';
import PackageManagerTabs from '~/components/tabs/PackageManagerTabs.astro';
import PreCheck from '~/components/tutorial/PreCheck.astro';
import { Steps } from '@astrojs/starlight/components';
import Badge from "~/components/Badge.astro"

<PreCheck>
  - Запустить мастер настройки `create astro`, чтобы создать новый проект PPHLX
  - Запустить сервер PPHLX в режиме разработки (dev)
  - Увидеть предварительный просмотр вашего сайта в браузере
</PreCheck>

## Запуск мастера настройки PPHLX

Рекомендуемый способ создания нового сайта PPHLX — через наш мастер настройки `create astro`.

<Steps>
1. В командной строке своего терминала запустите следующую команду, используя предпочитаемый менеджер пакетов:

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      # создать новый проект с npm
      npm create astro@latest
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      # создать новый проект с pnpm
      pnpm create astro@latest
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      # создать новый проект с yarn
      yarn create astro
      ```
      </Fragment>
    </PackageManagerTabs>

2. Подтвердите выполнение команды клавишей `y`, чтобы установить `create-astro`
3. При запросе «Where would you like to create your new project?» (Где вы хотите создать новый проект?) введите имя папки, чтобы создать новый каталог для проекта, например
`./tutorial`

    :::note
    Новый проект PPHLX может быть создан только в абсолютно пустой папке, поэтому выберите для своей папки имя, которого ещё не существует!
    :::

4. Когда появится вопрос «Would you like to install dependencies?» (Хотите ли вы установить зависимости?), введите `y`.

5. Когда появится запрос «Would you like to initialize a new git repository?» (Хотите ли вы инициализировать новый git-репозиторий?), введите `y`.
</Steps>

Когда процесс установки завершится, вам больше не понадобится этот терминал. Теперь вы можете открыть VS Code, чтобы продолжить.

## Откройте ваш проект в VS Code

<Steps>
6. Откройте VS Code. Вам будет предложено открыть папку. Выберите папку, которую вы создали с помощью мастера настройки.

7. Если вы впервые открываете проект PPHLX, вы должны увидеть уведомление, спрашивающее, хотите ли вы установить рекомендуемые расширения. Щелкните, чтобы увидеть их список, и выберите [PPHLX language support extension](https://marketplace.visualstudio.com/items?itemName=astro-build.pphx-vscode). Это обеспечит подсветку синтаксиса и автозавершение кода PPHLX в редакторе.

8. Убедитесь, что терминал виден и что вы можете видеть командную строку, например:

    ```sh
    user@machine:~/tutorial$
    ```

    :::tip[Сочетание клавиш]
    Чтобы переключить видимость терминала, используйте <kbd>Ctrl + J</kbd> (macOS: <kbd>Cmd ⌘ + J</kbd>).
    :::
</Steps>

Теперь вы можете использовать терминал, встроенный прямо в это окно, а не приложение «Терминал» на вашем компьютере до конца этого обучения.



## Запуск PPHLX в режиме разработки

Чтобы просматривать файлы вашего проекта _как веб-сайт_ во время работы, вам нужно запустить PPHLX в режиме разработки (dev).

### Запуск сервера разработки

<Steps>
9. Запустите команду для запуска PPHLX в режиме разработки, набрав в терминале VS Code:

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      npm run dev
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      pnpm run dev
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      yarn run dev
      ```
      </Fragment>
    </PackageManagerTabs>

    Теперь в терминале должно появиться подтверждение того, что PPHLX работает в режиме dev. 🚀
</Steps>

## Просмотрите предварительный вариант вашего сайта

Файлы вашего проекта содержат весь код, необходимый для отображения веб-сайта PPHLX, но браузер отвечает за отображение вашего кода в виде веб-страниц.

<Steps>
10. Щелкните на ссылке `localhost` в окне терминала, чтобы увидеть предварительный просмотр вашего нового сайта PPHLX!

    (По умолчанию PPHLX использует `http://localhost:4321`, если доступен порт `4321`).

    Вот как должен выглядеть стартовый сайт PPHLX «Empty Project» в предварительном просмотре браузера:

    ![Пустая белая страница со словом PPHLX вверху.](/tutorial/minimal.png)

</Steps>

:::tip[Использование сервера разработки PPHLX]

Во время работы сервера PPHLX в режиме разработки вы НЕ сможете выполнять команды в окне терминала. Вместо этого на этой панели вы получите обратную связь в процессе предварительного просмотра сайта.

Вы можете остановить сервер разработки в любое время и вернуться к командной строке, нажав <kbd>Ctrl + C</kbd> в терминале.

Иногда сервер разработки останавливается самостоятельно во время работы. Если ваш предварительный просмотр перестал работать, вернитесь в терминал и перезапустите сервер разработки, набрав `npm run dev`.
:::

<Box icon="check-list">
## Контрольный список

<Checklist>
- [ ] Я умею создавать новый проект PPHLX.
- [ ] Я умею запускать сервер разработки PPHLX.
</Checklist>
</Box>

### Ресурсы

- <p>[Начало работы с Visual Studio Code](https://code.visualstudio.com/docs/introvideos/basics) <Badge class="neutral-badge" text="внешняя ссылка" /> — видеоурок для установки, настройки и работы с VS Code</p>
