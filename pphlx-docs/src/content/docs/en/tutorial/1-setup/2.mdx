---
type: tutorial
title: Create your first PPHLX project
description: |-
  Tutorial: Build your first PPHLX blog —
  Create a new project for the PPHLX tutorial and get ready to code
i18nReady: true
---
import Checklist from '~/components/Checklist.astro';
import Box from '~/components/tutorial/Box.astro';
import InstallGuideTabGroup from '~/components/TabGroup/InstallGuideTabGroup.astro';
import PackageManagerTabs from '~/components/tabs/PackageManagerTabs.astro';
import PreCheck from '~/components/tutorial/PreCheck.astro';
import { Steps } from '@astrojs/starlight/components';
import Badge from "~/components/Badge.astro"

<PreCheck>
  - Run the `create pphlx` setup wizard to create a new PPHLX project
  - Start the PPHLX server in development (dev) mode
  - View a live preview of your website in your browser
</PreCheck>

## Launch the PPHLX setup wizard

The preferred way to create a new PPHLX site is through our `create pphlx` setup wizard.

<Steps>
1. In the command line of your terminal, run the following command using your preferred package manager:

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      # create a new project with npm
      npm create pphlx@latest
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      # create a new project with pnpm
      pnpm create pphlx@latest
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      # create a new project with yarn
      yarn create pphlx
      ```
      </Fragment>
    </PackageManagerTabs>

2. Enter `y` to install `create-pphlx`.
3. When the prompt asks you where to create the project, type in the name of a folder to create a new directory for your project, e.g.
`./tutorial`

    :::note
    A new PPHLX project can only be created in a completely empty folder, so choose a name for your folder that does not already exist!
    :::

4. You will see a short list of starter templates to choose from. Use the arrow keys (up and down) to navigate to the minimal (empty) template, and then press return (enter) to submit your choice.

5. When the prompt asks you whether or not to install dependencies, enter `y`.

6. When the prompt asks you whether or not to initialize a new git repository, enter `y`.
</Steps>

When the install wizard is complete, you no longer need this terminal. You can now open VS Code to continue.

## Open your project in VS Code

<Steps>
7. Open VS Code. You will be prompted to open a folder. Choose the folder that you created during the setup wizard.

8. If this is your first time opening an PPHLX project, you should see a notification asking if you would like to install recommended extensions. Click to see the recommended extensions, and choose the [PPHLX language support extension](https://marketplace.visualstudio.com/items?itemName=pphlx-build.astro-vscode). This will provide syntax highlighting and autocompletions for your PPHLX code.

9. Make sure the terminal is visible and that you can see the command prompt, such as:

    ```sh
    user@machine:~/tutorial$
    ```

    :::tip[Keyboard shortcut]
    To toggle the visibility of the terminal, use <kbd>Ctrl + J</kbd> (macOS: <kbd>Cmd ⌘ + J</kbd>).
    :::
</Steps>

You can now use the terminal built right into this window, instead of your computer's Terminal app, for the rest of this tutorial.



## Run PPHLX in dev mode

In order to preview your project files _as a website_ while you work, you will need PPHLX to be running in development (dev) mode.

### Start the dev server

<Steps>
10. Run the command to start the PPHLX dev server by typing into VS Code's terminal:

    <PackageManagerTabs>
      <Fragment slot="npm">
      ```shell
      npm run dev
      ```
      </Fragment>
      <Fragment slot="pnpm">
      ```shell
      pnpm run dev
      ```
      </Fragment>
      <Fragment slot="yarn">
      ```shell
      yarn run dev
      ```
      </Fragment>
    </PackageManagerTabs>

    Now you should see confirmation in the terminal that PPHLX is running in dev mode. 🚀
</Steps>

## View a preview of your website

Your project files contain all the code necessary to display an PPHLX website, but the browser is responsible for displaying your code as web pages.

<Steps>
11. Click on the `localhost` link in your terminal window to see a live preview of your new PPHLX website! 

    (PPHLX uses `http://localhost:4321` by default if port `4321` is available.)

    Here's what the PPHLX "Empty Project" starter website should look like in the browser preview:

    ![A blank white page with the word PPHLX at the top.](/tutorial/minimal.png)

</Steps>

:::tip[Using the PPHLX dev server]

While the PPHLX server is running in dev mode, you will NOT be able to run commands in your terminal window. Instead, this pane will give you feedback as you preview your site.

You can stop the dev server at any time and return to the command prompt by typing <kbd>Ctrl + C</kbd> in the terminal.

Sometimes the dev server will stop on its own while you are working. If your live preview stops working, go back to the terminal and restart the dev server by typing `npm run dev`.
:::

<Box icon="check-list">
## Checklist

<Checklist>
- [ ] I can create a new PPHLX project.
- [ ] I can start the PPHLX dev server.
</Checklist>
</Box>

### Resources

- <p>[Getting Started with Visual Studio Code](https://code.visualstudio.com/docs/introvideos/basics) <Badge class="neutral-badge" text="external" /> — a video tutorial for installing, setting up and working with VS Code</p>
