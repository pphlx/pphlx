---
title: PPHLX.response.headers must not be reassigned.
i18nReady: true
githubURL: https://github.com/pphlx/astro/blob/main/packages/astro/src/core/errors/errors-data.ts
---

> **PPHLXResponseHeadersReassigned**: 可以向 `PPHLX.response.headers` 添加或删除单个头部，但不得将其整体替换为另一个 `Headers` 实例。

## 哪里出了问题？
当尝试将一个值设置为可用于 `PPHLX.response` 的 `ResponseInit` 对象上的 `headers` 字段时抛出此错误。
